# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the main branch
on:
  push:
    branches: [find-endpoint]
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: ./engine

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  lint:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Set up Node.js
      - name: Use Node.js 14.x
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - name: Install dependencies
        run: yarn install

      - name: Run linter check
        run: yarn run lint

  db:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Set up Node.js
      - name: Use Node.js 14.x
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - name: Install dependencies
        run: yarn install

      - name: Docker build and run --detach
        run: docker-compose up --build --detach

      - name: Docker ps -a
        run: docker ps -a

      # Waiting for listening port
      - name: Health
        run: sleep 5 && curl http://localhost:8088/health

      # Check that all migrations are applied
      # - name: Create
      #   run: curl http://localhost:8088/v1/db/create/__example__

      # - name: Check
      #   run: curl http://localhost:8088/v1/db/check/__example__
        
      - name: Find
        run: curl http://localhost:8088/v1/db/find/__example__

      - name: Delete
        run: curl http://localhost:8088/v1/db/create/__example__

      - name: Docker stop
        run: docker stop $(docker ps -q)
