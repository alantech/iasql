{
  "name": "iasql",
  "version": "0.0.1",
  "description": "Infrastructure as SQL",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rm -rf dist",
    "lint": "tslint --project .",
    "test": "jest -i",
    "coverage": "jest -i --coverage",
    "start": "export $(cat .env | xargs) && if [ \"$IASQL_ENV\" = \"local\" ]; then yarn start:dev; else yarn serve; fi",
    "serve": "node --max_old_space_size=8192 dist/index.js",
    "start:dev": "nodemon -e ts -w ./src -x yarn start:dev:watch",
    "start:dev:watch": "yarn build && yarn serve",
    "gen-sql": "ts-node ./node_modules/.bin/typeorm migration:generate -n",
    "migrate": "ts-node ./node_modules/.bin/typeorm migration:run",
    "docker-compose:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "docker-compose": "docker-compose -f docker-compose.yml up --build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alantech/iasql.git"
  },
  "keywords": [
    "skybase",
    "iasql",
    "devops",
    "iac"
  ],
  "author": "David Ellis <david@alantechnologies.com>",
  "bugs": {
    "url": "https://github.com/alantech/iasql/issues"
  },
  "homepage": "https://iasql.com",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.9.1",
    "jest": "^27.2.5",
    "nodemon": "^2.0.13",
    "ts-jest": "^27.0.5",
    "ts-node-dev": "^1.1.8",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.31.0",
    "@aws-sdk/client-ecr": "^3.37.0",
    "@aws-sdk/client-ecs": "^3.37.0",
    "@aws-sdk/client-rds": "^3.35.0",
    "@aws-sdk/util-waiter": "^3.32.0",
    "@types/express": "^4.17.13",
    "@types/express-jwt": "^6.0.2",
    "axios": "^0.23.0",
    "express": "^4.17.1",
    "express-jwt": "^6.1.0",
    "jwks-rsa": "^2.0.4",
    "memoize-cache-decorator": "^1.3.2",
    "pg": "^8.7.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.37",
    "typeorm-naming-strategies": "^2.0.0"
  }
}
